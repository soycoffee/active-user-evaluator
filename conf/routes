#GET / controllers.ApiController.index(projectId: String, apiKey: String, authors: String, sinceBeforeDays: Int)
GET / controllers.ApiController.index()

GET /debug/backlog/users controllers.debug.BacklogController.queryUsers(projectId: String, apiKey: String)
GET /debug/backlog/activities controllers.debug.BacklogController.queryActivities(userId: Long, apiKey: String)
